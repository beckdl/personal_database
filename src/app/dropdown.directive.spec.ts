import { Dropdown } from './dropdown.directive';

describe('Dropdown', () => {
  it('should create an instance', () => {
    const directive = new Dropdown();
    expect(directive).toBeTruthy();
  });
});
